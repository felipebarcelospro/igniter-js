# External Transports

This example shows how to configure third-party Pino transports such as `pino-pretty` and external SaaS loggers.

Note: Install the transport dependencies before running this example.

Note: Until public exports are wired, this example uses internal imports from `src/`.
