# File Logging

This example configures a file transport and writes log entries to a local file, then flushes the logger before exit.

Note: Until public exports are wired, this example uses internal imports from `src/`.
