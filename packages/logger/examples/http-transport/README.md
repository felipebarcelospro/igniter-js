# HTTP Transport

This example configures the built-in `http` transport options for remote logging targets.

Note: The current HTTP transport resolver maps to `pino/file`, so this example documents configuration rather than actual HTTP ingestion.

Note: Until public exports are wired, this example uses internal imports from `src/`.
