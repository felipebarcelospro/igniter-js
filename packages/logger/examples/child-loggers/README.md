# Child Loggers

This example demonstrates how to create child loggers with scoped context for HTTP and database operations.

Note: Until public exports are wired, this example uses internal imports from `src/`.
