# Basic Usage

This example shows how to create a logger with default settings and use the core log methods, including `success()` and grouped output.

Note: Until public exports are wired, this example uses internal imports from `src/`.
