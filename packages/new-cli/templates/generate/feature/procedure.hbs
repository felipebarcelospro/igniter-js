{{!--
  @generated by @igniter-js/new-cli
  @description Basic procedure scaffold for the {{featureName}} feature
--}}
import { igniter } from '@/igniter'

export const {{procedureVariable}} = igniter.procedure({
  name: '{{procedureName}}',
  handler: async (_options, { context }) => {
    return {
      {{procedureContextKey}}: {
        example: () => {
          return {
            message: 'Hello from {{procedureName}}!',
            timestamp: new Date().toISOString(),
          }
        },
      },
    }
  },
})
