{{!--
  @generated by @igniter-js/new-cli
  @description Basic controller scaffold for a new feature
--}}
import { igniter } from '@/igniter'

export const {{controllerVariable}} = igniter.controller({
  name: '{{controllerName}}',
  path: '/{{controllerRoute}}',
  actions: {
    hello: igniter.query({
      path: '/hello',
      handler: async ({ response }) => {
        return response.success({ message: 'Hello from {{controllerName}}!' })
      },
    }),
  },
})
