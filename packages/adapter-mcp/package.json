{
  "name": "@igniter-js/adapter-mcp-server",
  "version": "0.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsup src/index.ts --dts --format esm,cjs --out-dir dist --watch",
    "build": "tsup src/index.ts --dts --format esm,cjs --out-dir dist"
  },
  "devDependencies": {
    "@igniter-js/core": "*",
    "@vercel/mcp-adapter": "^0.2.0",
    "@modelcontextprotocol/sdk": "^1.10.2"
  },
  "peerDependencies": {
    "@igniter-js/core": "*",
    "@vercel/mcp-adapter": "^0.2.0",
    "@modelcontextprotocol/sdk": "^1.10.2"
  },
  "license": "MIT"
}